name = "KKKIIO/selene_xaml"

version = "0.37.3"

import {
  "Milky2018/xml@0.4.0",
  "moonbitlang/async@0.20.2",
  "moonbitlang/x@0.4.45",
}

supported_targets = "native+js"

preferred_target = "native"

readme = "README.md"

repository = "https://github.com/kkkiio/selene.git"

license = "Apache-2.0"

keywords = [ "XAML", "UI", "ECS", "Game Engine" ]

description = "MS-XAML-2017 object-mapping profile and typed View generation for Selene"

source = "src"
