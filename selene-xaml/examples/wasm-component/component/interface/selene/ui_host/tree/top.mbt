// Generated by `wit-bindgen` 0.59.0. DO NOT EDIT!
///|
pub(all) enum NodeKind {
      VIEW; FLEX; TEXT; IMAGE; BUTTON; SCROLL_VIEW
} derive(Debug, Eq)

pub fn NodeKind::ordinal(self : NodeKind) -> Int {
      match self {
            VIEW => 0
            FLEX => 1
            TEXT => 2
            IMAGE => 3
            BUTTON => 4
            SCROLL_VIEW => 5
      }
}

pub fn NodeKind::from(self : Int) -> NodeKind {
      match self {
            0 => VIEW
            1 => FLEX
            2 => TEXT
            3 => IMAGE
            4 => BUTTON
            5 => SCROLL_VIEW
            _ => panic()
      }
}
///|
pub(all) enum TextAlign {
      LEFT; CENTER; RIGHT
} derive(Debug, Eq)

pub fn TextAlign::ordinal(self : TextAlign) -> Int {
      match self {
            LEFT => 0
            CENTER => 1
            RIGHT => 2
      }
}

pub fn TextAlign::from(self : Int) -> TextAlign {
      match self {
            0 => LEFT
            1 => CENTER
            2 => RIGHT
            _ => panic()
      }
}
///|
pub(all) enum ImageFit {
      FILL; CONTAIN; COVER
} derive(Debug, Eq)

pub fn ImageFit::ordinal(self : ImageFit) -> Int {
      match self {
            FILL => 0
            CONTAIN => 1
            COVER => 2
      }
}

pub fn ImageFit::from(self : Int) -> ImageFit {
      match self {
            0 => FILL
            1 => CONTAIN
            2 => COVER
            _ => panic()
      }
}
///|
pub(all) enum FlexDirection {
      ROW; ROW_REVERSE; COLUMN; COLUMN_REVERSE
} derive(Debug, Eq)

pub fn FlexDirection::ordinal(self : FlexDirection) -> Int {
      match self {
            ROW => 0
            ROW_REVERSE => 1
            COLUMN => 2
            COLUMN_REVERSE => 3
      }
}

pub fn FlexDirection::from(self : Int) -> FlexDirection {
      match self {
            0 => ROW
            1 => ROW_REVERSE
            2 => COLUMN
            3 => COLUMN_REVERSE
            _ => panic()
      }
}
///|
pub(all) enum LayoutValue {
      Auto
      Px(Float)
      Percent(Float)
} derive(Debug, Eq)
///|
pub(all) enum VisualStateValue {
      StringValue(String)
      BoolValue(Bool)
      DoubleValue(Double)
      IntValue(Int)
      LayoutValueValue(LayoutValue)
} derive(Debug, Eq)
///|
pub(all) struct VisualStateSetter {
      target : String; property : String; value : VisualStateValue
} derive(Debug, Eq)
///|
pub(all) struct VisualStateDefinition {
      name : String; setters : Array[VisualStateSetter]
} derive(Debug, Eq)
///|
pub(all) struct VisualTransition {
      from : String?; to : String?; duration : Double; delay : Double; easing : String
} derive(Debug, Eq)
///|
pub(all) struct VisualStateGroup {
      order : Int; name : String; current : String; interaction : Bool; states : Array[VisualStateDefinition]; transitions : Array[VisualTransition]
} derive(Debug, Eq)
///|
pub(all) struct NodeStyle {
      absolute : Bool; left : Float; top : Float; width : LayoutValue; height : LayoutValue; margin_left : Float; margin_top : Float; margin_right : Float; margin_bottom : Float; padding_left : Float; padding_top : Float; padding_right : Float; padding_bottom : Float; background : String; border_color : String; border : Float; radius : Float; outline_color : String; outline_width : Float; outline_offset : Float; color : String; font_family : String; font_size : Float; bold : Bool; align : TextAlign; wrap : Bool; z_index : Int; fit : ImageFit; direction : FlexDirection; flex_basis : LayoutValue; flex_grow : Float; flex_shrink : Float; row_gap : Float; column_gap : Float; horizontal_scroll : Bool; vertical_scroll : Bool; scrollbar_width : Float
} derive(Debug, Eq)
///|
pub(all) struct NodeSpec {
      key : String; parent : String?; kind : NodeKind; text : String; source : String; active : Bool; enabled : Bool; focusable : Bool; style : NodeStyle; visual_state_groups : Array[VisualStateGroup]
} derive(Debug, Eq)
///|
pub(all) struct TextMutation {
      key : String; value : String
} derive(Debug, Eq)
///|
pub(all) struct BoolMutation {
      key : String; value : Bool
} derive(Debug, Eq)
///|
pub(all) struct F32Mutation {
      key : String; value : Float
} derive(Debug, Eq)
///|
pub(all) struct MoveMutation {
      key : String; parent : String; index : UInt
} derive(Debug, Eq)
///|
pub(all) enum TreeMutation {
      Mount(NodeSpec)
      Update(NodeSpec)
      SetText(TextMutation)
      SetSource(TextMutation)
      SetBackground(TextMutation)
      SetActive(BoolMutation)
      SetEnabled(BoolMutation)
      SetTop(F32Mutation)
      Move(MoveMutation)
      Remove(String)
} derive(Debug, Eq)
///|
pub(all) struct HostError {
      code : String; message : String
} derive(Debug, Eq)
///|
pub fn apply(entity : UInt, mutations : Array[TreeMutation]) -> Result[Unit, HostError] {
      let cleanup_list : Array[Int] = []

      let address116 = mbt_ffi_malloc((mutations).length() * 220);
      for index117 = 0; index117 < (mutations).length(); index117 = index117 + 1 {
            let iter_elem : TreeMutation = (mutations)[(index117)]
            let iter_base = address116 + (index117 * 220);

            match iter_elem {
                  Mount(payload) => {
                        mbt_ffi_store8((iter_base) + 0, (0))

                        let ptr = mbt_ffi_str2ptr((payload).key)
                        mbt_ffi_store32((iter_base) + 8, (payload).key.length())
                        mbt_ffi_store32((iter_base) + 4, ptr)

                        match ((payload).parent) {
                              None => {
                                    mbt_ffi_store8((iter_base) + 12, (0))

                                    ()
                              }
                              Some(payload1) => {
                                    mbt_ffi_store8((iter_base) + 12, (1))

                                    let ptr2 = mbt_ffi_str2ptr(payload1)
                                    mbt_ffi_store32((iter_base) + 20, payload1.length())
                                    mbt_ffi_store32((iter_base) + 16, ptr2)
                                    cleanup_list.push(ptr2)

                                    ()
                              }
                        }
                        mbt_ffi_store8((iter_base) + 24, (payload).kind.ordinal())

                        let ptr3 = mbt_ffi_str2ptr((payload).text)
                        mbt_ffi_store32((iter_base) + 32, (payload).text.length())
                        mbt_ffi_store32((iter_base) + 28, ptr3)

                        let ptr4 = mbt_ffi_str2ptr((payload).source)
                        mbt_ffi_store32((iter_base) + 40, (payload).source.length())
                        mbt_ffi_store32((iter_base) + 36, ptr4)
                        mbt_ffi_store8((iter_base) + 44, (if (payload).active { 1 } else { 0 }))
                        mbt_ffi_store8((iter_base) + 45, (if (payload).enabled { 1 } else { 0 }))
                        mbt_ffi_store8((iter_base) + 46, (if (payload).focusable { 1 } else { 0 }))
                        mbt_ffi_store8((iter_base) + 48, (if ((payload).style).absolute { 1 } else { 0 }))
                        mbt_ffi_storef32((iter_base) + 52, ((payload).style).left)
                        mbt_ffi_storef32((iter_base) + 56, ((payload).style).top)

                        match ((payload).style).width {
                              Auto => {
                                    mbt_ffi_store8((iter_base) + 60, (0))

                                    ()
                              }
                              Px(payload6) => {
                                    mbt_ffi_store8((iter_base) + 60, (1))
                                    mbt_ffi_storef32((iter_base) + 64, payload6)

                                    ()
                              }
                              Percent(payload7) => {
                                    mbt_ffi_store8((iter_base) + 60, (2))
                                    mbt_ffi_storef32((iter_base) + 64, payload7)

                                    ()
                              }
                        }

                        match ((payload).style).height {
                              Auto => {
                                    mbt_ffi_store8((iter_base) + 68, (0))

                                    ()
                              }
                              Px(payload9) => {
                                    mbt_ffi_store8((iter_base) + 68, (1))
                                    mbt_ffi_storef32((iter_base) + 72, payload9)

                                    ()
                              }
                              Percent(payload10) => {
                                    mbt_ffi_store8((iter_base) + 68, (2))
                                    mbt_ffi_storef32((iter_base) + 72, payload10)

                                    ()
                              }
                        }
                        mbt_ffi_storef32((iter_base) + 76, ((payload).style).margin_left)
                        mbt_ffi_storef32((iter_base) + 80, ((payload).style).margin_top)
                        mbt_ffi_storef32((iter_base) + 84, ((payload).style).margin_right)
                        mbt_ffi_storef32((iter_base) + 88, ((payload).style).margin_bottom)
                        mbt_ffi_storef32((iter_base) + 92, ((payload).style).padding_left)
                        mbt_ffi_storef32((iter_base) + 96, ((payload).style).padding_top)
                        mbt_ffi_storef32((iter_base) + 100, ((payload).style).padding_right)
                        mbt_ffi_storef32((iter_base) + 104, ((payload).style).padding_bottom)

                        let ptr11 = mbt_ffi_str2ptr(((payload).style).background)
                        mbt_ffi_store32((iter_base) + 112, ((payload).style).background.length())
                        mbt_ffi_store32((iter_base) + 108, ptr11)

                        let ptr12 = mbt_ffi_str2ptr(((payload).style).border_color)
                        mbt_ffi_store32((iter_base) + 120, ((payload).style).border_color.length())
                        mbt_ffi_store32((iter_base) + 116, ptr12)
                        mbt_ffi_storef32((iter_base) + 124, ((payload).style).border)
                        mbt_ffi_storef32((iter_base) + 128, ((payload).style).radius)

                        let ptr13 = mbt_ffi_str2ptr(((payload).style).outline_color)
                        mbt_ffi_store32((iter_base) + 136, ((payload).style).outline_color.length())
                        mbt_ffi_store32((iter_base) + 132, ptr13)
                        mbt_ffi_storef32((iter_base) + 140, ((payload).style).outline_width)
                        mbt_ffi_storef32((iter_base) + 144, ((payload).style).outline_offset)

                        let ptr14 = mbt_ffi_str2ptr(((payload).style).color)
                        mbt_ffi_store32((iter_base) + 152, ((payload).style).color.length())
                        mbt_ffi_store32((iter_base) + 148, ptr14)

                        let ptr15 = mbt_ffi_str2ptr(((payload).style).font_family)
                        mbt_ffi_store32((iter_base) + 160, ((payload).style).font_family.length())
                        mbt_ffi_store32((iter_base) + 156, ptr15)
                        mbt_ffi_storef32((iter_base) + 164, ((payload).style).font_size)
                        mbt_ffi_store8((iter_base) + 168, (if ((payload).style).bold { 1 } else { 0 }))
                        mbt_ffi_store8((iter_base) + 169, ((payload).style).align.ordinal())
                        mbt_ffi_store8((iter_base) + 170, (if ((payload).style).wrap { 1 } else { 0 }))
                        mbt_ffi_store32((iter_base) + 172, ((payload).style).z_index)
                        mbt_ffi_store8((iter_base) + 176, ((payload).style).fit.ordinal())
                        mbt_ffi_store8((iter_base) + 177, ((payload).style).direction.ordinal())

                        match ((payload).style).flex_basis {
                              Auto => {
                                    mbt_ffi_store8((iter_base) + 180, (0))

                                    ()
                              }
                              Px(payload17) => {
                                    mbt_ffi_store8((iter_base) + 180, (1))
                                    mbt_ffi_storef32((iter_base) + 184, payload17)

                                    ()
                              }
                              Percent(payload18) => {
                                    mbt_ffi_store8((iter_base) + 180, (2))
                                    mbt_ffi_storef32((iter_base) + 184, payload18)

                                    ()
                              }
                        }
                        mbt_ffi_storef32((iter_base) + 188, ((payload).style).flex_grow)
                        mbt_ffi_storef32((iter_base) + 192, ((payload).style).flex_shrink)
                        mbt_ffi_storef32((iter_base) + 196, ((payload).style).row_gap)
                        mbt_ffi_storef32((iter_base) + 200, ((payload).style).column_gap)
                        mbt_ffi_store8((iter_base) + 204, (if ((payload).style).horizontal_scroll { 1 } else { 0 }))
                        mbt_ffi_store8((iter_base) + 205, (if ((payload).style).vertical_scroll { 1 } else { 0 }))
                        mbt_ffi_storef32((iter_base) + 208, ((payload).style).scrollbar_width)

                        let address44 = mbt_ffi_malloc(((payload).visual_state_groups).length() * 40);
                        for index45 = 0; index45 < ((payload).visual_state_groups).length(); index45 = index45 + 1 {
                              let iter_elem : VisualStateGroup = ((payload).visual_state_groups)[(index45)]
                              let iter_base = address44 + (index45 * 40);
                              mbt_ffi_store32((iter_base) + 0, (iter_elem).order)

                              let ptr19 = mbt_ffi_str2ptr((iter_elem).name)
                              mbt_ffi_store32((iter_base) + 8, (iter_elem).name.length())
                              mbt_ffi_store32((iter_base) + 4, ptr19)

                              let ptr20 = mbt_ffi_str2ptr((iter_elem).current)
                              mbt_ffi_store32((iter_base) + 16, (iter_elem).current.length())
                              mbt_ffi_store32((iter_base) + 12, ptr20)
                              mbt_ffi_store8((iter_base) + 20, (if (iter_elem).interaction { 1 } else { 0 }))

                              let address33 = mbt_ffi_malloc(((iter_elem).states).length() * 16);
                              for index34 = 0; index34 < ((iter_elem).states).length(); index34 = index34 + 1 {
                                    let iter_elem : VisualStateDefinition = ((iter_elem).states)[(index34)]
                                    let iter_base = address33 + (index34 * 16);

                                    let ptr21 = mbt_ffi_str2ptr((iter_elem).name)
                                    mbt_ffi_store32((iter_base) + 4, (iter_elem).name.length())
                                    mbt_ffi_store32((iter_base) + 0, ptr21)

                                    let address = mbt_ffi_malloc(((iter_elem).setters).length() * 32);
                                    for index = 0; index < ((iter_elem).setters).length(); index = index + 1 {
                                          let iter_elem : VisualStateSetter = ((iter_elem).setters)[(index)]
                                          let iter_base = address + (index * 32);

                                          let ptr22 = mbt_ffi_str2ptr((iter_elem).target)
                                          mbt_ffi_store32((iter_base) + 4, (iter_elem).target.length())
                                          mbt_ffi_store32((iter_base) + 0, ptr22)

                                          let ptr23 = mbt_ffi_str2ptr((iter_elem).property)
                                          mbt_ffi_store32((iter_base) + 12, (iter_elem).property.length())
                                          mbt_ffi_store32((iter_base) + 8, ptr23)

                                          match (iter_elem).value {
                                                StringValue(payload24) => {
                                                      mbt_ffi_store8((iter_base) + 16, (0))

                                                      let ptr25 = mbt_ffi_str2ptr(payload24)
                                                      mbt_ffi_store32((iter_base) + 28, payload24.length())
                                                      mbt_ffi_store32((iter_base) + 24, ptr25)
                                                      cleanup_list.push(ptr25)

                                                      ()
                                                }
                                                BoolValue(payload26) => {
                                                      mbt_ffi_store8((iter_base) + 16, (1))
                                                      mbt_ffi_store8((iter_base) + 24, (if payload26 { 1 } else { 0 }))

                                                      ()
                                                }
                                                DoubleValue(payload27) => {
                                                      mbt_ffi_store8((iter_base) + 16, (2))
                                                      mbt_ffi_storef64((iter_base) + 24, payload27)

                                                      ()
                                                }
                                                IntValue(payload28) => {
                                                      mbt_ffi_store8((iter_base) + 16, (3))
                                                      mbt_ffi_store32((iter_base) + 24, payload28)

                                                      ()
                                                }
                                                LayoutValueValue(payload29) => {
                                                      mbt_ffi_store8((iter_base) + 16, (4))

                                                      match payload29 {
                                                            Auto => {
                                                                  mbt_ffi_store8((iter_base) + 24, (0))

                                                                  ()
                                                            }
                                                            Px(payload31) => {
                                                                  mbt_ffi_store8((iter_base) + 24, (1))
                                                                  mbt_ffi_storef32((iter_base) + 28, payload31)

                                                                  ()
                                                            }
                                                            Percent(payload32) => {
                                                                  mbt_ffi_store8((iter_base) + 24, (2))
                                                                  mbt_ffi_storef32((iter_base) + 28, payload32)

                                                                  ()
                                                            }
                                                      }

                                                      ()
                                                }
                                          }
                                          cleanup_list.push(ptr22)
                                          cleanup_list.push(ptr23)

                                    }
                                    mbt_ffi_store32((iter_base) + 12, ((iter_elem).setters).length())
                                    mbt_ffi_store32((iter_base) + 8, address)
                                    cleanup_list.push(ptr21)
                                    cleanup_list.push(address)

                              }
                              mbt_ffi_store32((iter_base) + 28, ((iter_elem).states).length())
                              mbt_ffi_store32((iter_base) + 24, address33)

                              let address42 = mbt_ffi_malloc(((iter_elem).transitions).length() * 48);
                              for index43 = 0; index43 < ((iter_elem).transitions).length(); index43 = index43 + 1 {
                                    let iter_elem : VisualTransition = ((iter_elem).transitions)[(index43)]
                                    let iter_base = address42 + (index43 * 48);

                                    match ((iter_elem).from) {
                                          None => {
                                                mbt_ffi_store8((iter_base) + 0, (0))

                                                ()
                                          }
                                          Some(payload36) => {
                                                mbt_ffi_store8((iter_base) + 0, (1))

                                                let ptr37 = mbt_ffi_str2ptr(payload36)
                                                mbt_ffi_store32((iter_base) + 8, payload36.length())
                                                mbt_ffi_store32((iter_base) + 4, ptr37)
                                                cleanup_list.push(ptr37)

                                                ()
                                          }
                                    }

                                    match ((iter_elem).to) {
                                          None => {
                                                mbt_ffi_store8((iter_base) + 12, (0))

                                                ()
                                          }
                                          Some(payload39) => {
                                                mbt_ffi_store8((iter_base) + 12, (1))

                                                let ptr40 = mbt_ffi_str2ptr(payload39)
                                                mbt_ffi_store32((iter_base) + 20, payload39.length())
                                                mbt_ffi_store32((iter_base) + 16, ptr40)
                                                cleanup_list.push(ptr40)

                                                ()
                                          }
                                    }
                                    mbt_ffi_storef64((iter_base) + 24, (iter_elem).duration)
                                    mbt_ffi_storef64((iter_base) + 32, (iter_elem).delay)

                                    let ptr41 = mbt_ffi_str2ptr((iter_elem).easing)
                                    mbt_ffi_store32((iter_base) + 44, (iter_elem).easing.length())
                                    mbt_ffi_store32((iter_base) + 40, ptr41)
                                    cleanup_list.push(ptr41)

                              }
                              mbt_ffi_store32((iter_base) + 36, ((iter_elem).transitions).length())
                              mbt_ffi_store32((iter_base) + 32, address42)
                              cleanup_list.push(ptr19)
                              cleanup_list.push(ptr20)
                              cleanup_list.push(address33)
                              cleanup_list.push(address42)

                        }
                        mbt_ffi_store32((iter_base) + 216, ((payload).visual_state_groups).length())
                        mbt_ffi_store32((iter_base) + 212, address44)
                        cleanup_list.push(ptr)
                        cleanup_list.push(ptr3)
                        cleanup_list.push(ptr4)
                        cleanup_list.push(ptr11)
                        cleanup_list.push(ptr12)
                        cleanup_list.push(ptr13)
                        cleanup_list.push(ptr14)
                        cleanup_list.push(ptr15)
                        cleanup_list.push(address44)

                        ()
                  }
                  Update(payload46) => {
                        mbt_ffi_store8((iter_base) + 0, (1))

                        let ptr47 = mbt_ffi_str2ptr((payload46).key)
                        mbt_ffi_store32((iter_base) + 8, (payload46).key.length())
                        mbt_ffi_store32((iter_base) + 4, ptr47)

                        match ((payload46).parent) {
                              None => {
                                    mbt_ffi_store8((iter_base) + 12, (0))

                                    ()
                              }
                              Some(payload49) => {
                                    mbt_ffi_store8((iter_base) + 12, (1))

                                    let ptr50 = mbt_ffi_str2ptr(payload49)
                                    mbt_ffi_store32((iter_base) + 20, payload49.length())
                                    mbt_ffi_store32((iter_base) + 16, ptr50)
                                    cleanup_list.push(ptr50)

                                    ()
                              }
                        }
                        mbt_ffi_store8((iter_base) + 24, (payload46).kind.ordinal())

                        let ptr51 = mbt_ffi_str2ptr((payload46).text)
                        mbt_ffi_store32((iter_base) + 32, (payload46).text.length())
                        mbt_ffi_store32((iter_base) + 28, ptr51)

                        let ptr52 = mbt_ffi_str2ptr((payload46).source)
                        mbt_ffi_store32((iter_base) + 40, (payload46).source.length())
                        mbt_ffi_store32((iter_base) + 36, ptr52)
                        mbt_ffi_store8((iter_base) + 44, (if (payload46).active { 1 } else { 0 }))
                        mbt_ffi_store8((iter_base) + 45, (if (payload46).enabled { 1 } else { 0 }))
                        mbt_ffi_store8((iter_base) + 46, (if (payload46).focusable { 1 } else { 0 }))
                        mbt_ffi_store8((iter_base) + 48, (if ((payload46).style).absolute { 1 } else { 0 }))
                        mbt_ffi_storef32((iter_base) + 52, ((payload46).style).left)
                        mbt_ffi_storef32((iter_base) + 56, ((payload46).style).top)

                        match ((payload46).style).width {
                              Auto => {
                                    mbt_ffi_store8((iter_base) + 60, (0))

                                    ()
                              }
                              Px(payload54) => {
                                    mbt_ffi_store8((iter_base) + 60, (1))
                                    mbt_ffi_storef32((iter_base) + 64, payload54)

                                    ()
                              }
                              Percent(payload55) => {
                                    mbt_ffi_store8((iter_base) + 60, (2))
                                    mbt_ffi_storef32((iter_base) + 64, payload55)

                                    ()
                              }
                        }

                        match ((payload46).style).height {
                              Auto => {
                                    mbt_ffi_store8((iter_base) + 68, (0))

                                    ()
                              }
                              Px(payload57) => {
                                    mbt_ffi_store8((iter_base) + 68, (1))
                                    mbt_ffi_storef32((iter_base) + 72, payload57)

                                    ()
                              }
                              Percent(payload58) => {
                                    mbt_ffi_store8((iter_base) + 68, (2))
                                    mbt_ffi_storef32((iter_base) + 72, payload58)

                                    ()
                              }
                        }
                        mbt_ffi_storef32((iter_base) + 76, ((payload46).style).margin_left)
                        mbt_ffi_storef32((iter_base) + 80, ((payload46).style).margin_top)
                        mbt_ffi_storef32((iter_base) + 84, ((payload46).style).margin_right)
                        mbt_ffi_storef32((iter_base) + 88, ((payload46).style).margin_bottom)
                        mbt_ffi_storef32((iter_base) + 92, ((payload46).style).padding_left)
                        mbt_ffi_storef32((iter_base) + 96, ((payload46).style).padding_top)
                        mbt_ffi_storef32((iter_base) + 100, ((payload46).style).padding_right)
                        mbt_ffi_storef32((iter_base) + 104, ((payload46).style).padding_bottom)

                        let ptr59 = mbt_ffi_str2ptr(((payload46).style).background)
                        mbt_ffi_store32((iter_base) + 112, ((payload46).style).background.length())
                        mbt_ffi_store32((iter_base) + 108, ptr59)

                        let ptr60 = mbt_ffi_str2ptr(((payload46).style).border_color)
                        mbt_ffi_store32((iter_base) + 120, ((payload46).style).border_color.length())
                        mbt_ffi_store32((iter_base) + 116, ptr60)
                        mbt_ffi_storef32((iter_base) + 124, ((payload46).style).border)
                        mbt_ffi_storef32((iter_base) + 128, ((payload46).style).radius)

                        let ptr61 = mbt_ffi_str2ptr(((payload46).style).outline_color)
                        mbt_ffi_store32((iter_base) + 136, ((payload46).style).outline_color.length())
                        mbt_ffi_store32((iter_base) + 132, ptr61)
                        mbt_ffi_storef32((iter_base) + 140, ((payload46).style).outline_width)
                        mbt_ffi_storef32((iter_base) + 144, ((payload46).style).outline_offset)

                        let ptr62 = mbt_ffi_str2ptr(((payload46).style).color)
                        mbt_ffi_store32((iter_base) + 152, ((payload46).style).color.length())
                        mbt_ffi_store32((iter_base) + 148, ptr62)

                        let ptr63 = mbt_ffi_str2ptr(((payload46).style).font_family)
                        mbt_ffi_store32((iter_base) + 160, ((payload46).style).font_family.length())
                        mbt_ffi_store32((iter_base) + 156, ptr63)
                        mbt_ffi_storef32((iter_base) + 164, ((payload46).style).font_size)
                        mbt_ffi_store8((iter_base) + 168, (if ((payload46).style).bold { 1 } else { 0 }))
                        mbt_ffi_store8((iter_base) + 169, ((payload46).style).align.ordinal())
                        mbt_ffi_store8((iter_base) + 170, (if ((payload46).style).wrap { 1 } else { 0 }))
                        mbt_ffi_store32((iter_base) + 172, ((payload46).style).z_index)
                        mbt_ffi_store8((iter_base) + 176, ((payload46).style).fit.ordinal())
                        mbt_ffi_store8((iter_base) + 177, ((payload46).style).direction.ordinal())

                        match ((payload46).style).flex_basis {
                              Auto => {
                                    mbt_ffi_store8((iter_base) + 180, (0))

                                    ()
                              }
                              Px(payload65) => {
                                    mbt_ffi_store8((iter_base) + 180, (1))
                                    mbt_ffi_storef32((iter_base) + 184, payload65)

                                    ()
                              }
                              Percent(payload66) => {
                                    mbt_ffi_store8((iter_base) + 180, (2))
                                    mbt_ffi_storef32((iter_base) + 184, payload66)

                                    ()
                              }
                        }
                        mbt_ffi_storef32((iter_base) + 188, ((payload46).style).flex_grow)
                        mbt_ffi_storef32((iter_base) + 192, ((payload46).style).flex_shrink)
                        mbt_ffi_storef32((iter_base) + 196, ((payload46).style).row_gap)
                        mbt_ffi_storef32((iter_base) + 200, ((payload46).style).column_gap)
                        mbt_ffi_store8((iter_base) + 204, (if ((payload46).style).horizontal_scroll { 1 } else { 0 }))
                        mbt_ffi_store8((iter_base) + 205, (if ((payload46).style).vertical_scroll { 1 } else { 0 }))
                        mbt_ffi_storef32((iter_base) + 208, ((payload46).style).scrollbar_width)

                        let address94 = mbt_ffi_malloc(((payload46).visual_state_groups).length() * 40);
                        for index95 = 0; index95 < ((payload46).visual_state_groups).length(); index95 = index95 + 1 {
                              let iter_elem : VisualStateGroup = ((payload46).visual_state_groups)[(index95)]
                              let iter_base = address94 + (index95 * 40);
                              mbt_ffi_store32((iter_base) + 0, (iter_elem).order)

                              let ptr67 = mbt_ffi_str2ptr((iter_elem).name)
                              mbt_ffi_store32((iter_base) + 8, (iter_elem).name.length())
                              mbt_ffi_store32((iter_base) + 4, ptr67)

                              let ptr68 = mbt_ffi_str2ptr((iter_elem).current)
                              mbt_ffi_store32((iter_base) + 16, (iter_elem).current.length())
                              mbt_ffi_store32((iter_base) + 12, ptr68)
                              mbt_ffi_store8((iter_base) + 20, (if (iter_elem).interaction { 1 } else { 0 }))

                              let address83 = mbt_ffi_malloc(((iter_elem).states).length() * 16);
                              for index84 = 0; index84 < ((iter_elem).states).length(); index84 = index84 + 1 {
                                    let iter_elem : VisualStateDefinition = ((iter_elem).states)[(index84)]
                                    let iter_base = address83 + (index84 * 16);

                                    let ptr69 = mbt_ffi_str2ptr((iter_elem).name)
                                    mbt_ffi_store32((iter_base) + 4, (iter_elem).name.length())
                                    mbt_ffi_store32((iter_base) + 0, ptr69)

                                    let address81 = mbt_ffi_malloc(((iter_elem).setters).length() * 32);
                                    for index82 = 0; index82 < ((iter_elem).setters).length(); index82 = index82 + 1 {
                                          let iter_elem : VisualStateSetter = ((iter_elem).setters)[(index82)]
                                          let iter_base = address81 + (index82 * 32);

                                          let ptr70 = mbt_ffi_str2ptr((iter_elem).target)
                                          mbt_ffi_store32((iter_base) + 4, (iter_elem).target.length())
                                          mbt_ffi_store32((iter_base) + 0, ptr70)

                                          let ptr71 = mbt_ffi_str2ptr((iter_elem).property)
                                          mbt_ffi_store32((iter_base) + 12, (iter_elem).property.length())
                                          mbt_ffi_store32((iter_base) + 8, ptr71)

                                          match (iter_elem).value {
                                                StringValue(payload72) => {
                                                      mbt_ffi_store8((iter_base) + 16, (0))

                                                      let ptr73 = mbt_ffi_str2ptr(payload72)
                                                      mbt_ffi_store32((iter_base) + 28, payload72.length())
                                                      mbt_ffi_store32((iter_base) + 24, ptr73)
                                                      cleanup_list.push(ptr73)

                                                      ()
                                                }
                                                BoolValue(payload74) => {
                                                      mbt_ffi_store8((iter_base) + 16, (1))
                                                      mbt_ffi_store8((iter_base) + 24, (if payload74 { 1 } else { 0 }))

                                                      ()
                                                }
                                                DoubleValue(payload75) => {
                                                      mbt_ffi_store8((iter_base) + 16, (2))
                                                      mbt_ffi_storef64((iter_base) + 24, payload75)

                                                      ()
                                                }
                                                IntValue(payload76) => {
                                                      mbt_ffi_store8((iter_base) + 16, (3))
                                                      mbt_ffi_store32((iter_base) + 24, payload76)

                                                      ()
                                                }
                                                LayoutValueValue(payload77) => {
                                                      mbt_ffi_store8((iter_base) + 16, (4))

                                                      match payload77 {
                                                            Auto => {
                                                                  mbt_ffi_store8((iter_base) + 24, (0))

                                                                  ()
                                                            }
                                                            Px(payload79) => {
                                                                  mbt_ffi_store8((iter_base) + 24, (1))
                                                                  mbt_ffi_storef32((iter_base) + 28, payload79)

                                                                  ()
                                                            }
                                                            Percent(payload80) => {
                                                                  mbt_ffi_store8((iter_base) + 24, (2))
                                                                  mbt_ffi_storef32((iter_base) + 28, payload80)

                                                                  ()
                                                            }
                                                      }

                                                      ()
                                                }
                                          }
                                          cleanup_list.push(ptr70)
                                          cleanup_list.push(ptr71)

                                    }
                                    mbt_ffi_store32((iter_base) + 12, ((iter_elem).setters).length())
                                    mbt_ffi_store32((iter_base) + 8, address81)
                                    cleanup_list.push(ptr69)
                                    cleanup_list.push(address81)

                              }
                              mbt_ffi_store32((iter_base) + 28, ((iter_elem).states).length())
                              mbt_ffi_store32((iter_base) + 24, address83)

                              let address92 = mbt_ffi_malloc(((iter_elem).transitions).length() * 48);
                              for index93 = 0; index93 < ((iter_elem).transitions).length(); index93 = index93 + 1 {
                                    let iter_elem : VisualTransition = ((iter_elem).transitions)[(index93)]
                                    let iter_base = address92 + (index93 * 48);

                                    match ((iter_elem).from) {
                                          None => {
                                                mbt_ffi_store8((iter_base) + 0, (0))

                                                ()
                                          }
                                          Some(payload86) => {
                                                mbt_ffi_store8((iter_base) + 0, (1))

                                                let ptr87 = mbt_ffi_str2ptr(payload86)
                                                mbt_ffi_store32((iter_base) + 8, payload86.length())
                                                mbt_ffi_store32((iter_base) + 4, ptr87)
                                                cleanup_list.push(ptr87)

                                                ()
                                          }
                                    }

                                    match ((iter_elem).to) {
                                          None => {
                                                mbt_ffi_store8((iter_base) + 12, (0))

                                                ()
                                          }
                                          Some(payload89) => {
                                                mbt_ffi_store8((iter_base) + 12, (1))

                                                let ptr90 = mbt_ffi_str2ptr(payload89)
                                                mbt_ffi_store32((iter_base) + 20, payload89.length())
                                                mbt_ffi_store32((iter_base) + 16, ptr90)
                                                cleanup_list.push(ptr90)

                                                ()
                                          }
                                    }
                                    mbt_ffi_storef64((iter_base) + 24, (iter_elem).duration)
                                    mbt_ffi_storef64((iter_base) + 32, (iter_elem).delay)

                                    let ptr91 = mbt_ffi_str2ptr((iter_elem).easing)
                                    mbt_ffi_store32((iter_base) + 44, (iter_elem).easing.length())
                                    mbt_ffi_store32((iter_base) + 40, ptr91)
                                    cleanup_list.push(ptr91)

                              }
                              mbt_ffi_store32((iter_base) + 36, ((iter_elem).transitions).length())
                              mbt_ffi_store32((iter_base) + 32, address92)
                              cleanup_list.push(ptr67)
                              cleanup_list.push(ptr68)
                              cleanup_list.push(address83)
                              cleanup_list.push(address92)

                        }
                        mbt_ffi_store32((iter_base) + 216, ((payload46).visual_state_groups).length())
                        mbt_ffi_store32((iter_base) + 212, address94)
                        cleanup_list.push(ptr47)
                        cleanup_list.push(ptr51)
                        cleanup_list.push(ptr52)
                        cleanup_list.push(ptr59)
                        cleanup_list.push(ptr60)
                        cleanup_list.push(ptr61)
                        cleanup_list.push(ptr62)
                        cleanup_list.push(ptr63)
                        cleanup_list.push(address94)

                        ()
                  }
                  SetText(payload96) => {
                        mbt_ffi_store8((iter_base) + 0, (2))

                        let ptr97 = mbt_ffi_str2ptr((payload96).key)
                        mbt_ffi_store32((iter_base) + 8, (payload96).key.length())
                        mbt_ffi_store32((iter_base) + 4, ptr97)

                        let ptr98 = mbt_ffi_str2ptr((payload96).value)
                        mbt_ffi_store32((iter_base) + 16, (payload96).value.length())
                        mbt_ffi_store32((iter_base) + 12, ptr98)
                        cleanup_list.push(ptr97)
                        cleanup_list.push(ptr98)

                        ()
                  }
                  SetSource(payload99) => {
                        mbt_ffi_store8((iter_base) + 0, (3))

                        let ptr100 = mbt_ffi_str2ptr((payload99).key)
                        mbt_ffi_store32((iter_base) + 8, (payload99).key.length())
                        mbt_ffi_store32((iter_base) + 4, ptr100)

                        let ptr101 = mbt_ffi_str2ptr((payload99).value)
                        mbt_ffi_store32((iter_base) + 16, (payload99).value.length())
                        mbt_ffi_store32((iter_base) + 12, ptr101)
                        cleanup_list.push(ptr100)
                        cleanup_list.push(ptr101)

                        ()
                  }
                  SetBackground(payload102) => {
                        mbt_ffi_store8((iter_base) + 0, (4))

                        let ptr103 = mbt_ffi_str2ptr((payload102).key)
                        mbt_ffi_store32((iter_base) + 8, (payload102).key.length())
                        mbt_ffi_store32((iter_base) + 4, ptr103)

                        let ptr104 = mbt_ffi_str2ptr((payload102).value)
                        mbt_ffi_store32((iter_base) + 16, (payload102).value.length())
                        mbt_ffi_store32((iter_base) + 12, ptr104)
                        cleanup_list.push(ptr103)
                        cleanup_list.push(ptr104)

                        ()
                  }
                  SetActive(payload105) => {
                        mbt_ffi_store8((iter_base) + 0, (5))

                        let ptr106 = mbt_ffi_str2ptr((payload105).key)
                        mbt_ffi_store32((iter_base) + 8, (payload105).key.length())
                        mbt_ffi_store32((iter_base) + 4, ptr106)
                        mbt_ffi_store8((iter_base) + 12, (if (payload105).value { 1 } else { 0 }))
                        cleanup_list.push(ptr106)

                        ()
                  }
                  SetEnabled(payload107) => {
                        mbt_ffi_store8((iter_base) + 0, (6))

                        let ptr108 = mbt_ffi_str2ptr((payload107).key)
                        mbt_ffi_store32((iter_base) + 8, (payload107).key.length())
                        mbt_ffi_store32((iter_base) + 4, ptr108)
                        mbt_ffi_store8((iter_base) + 12, (if (payload107).value { 1 } else { 0 }))
                        cleanup_list.push(ptr108)

                        ()
                  }
                  SetTop(payload109) => {
                        mbt_ffi_store8((iter_base) + 0, (7))

                        let ptr110 = mbt_ffi_str2ptr((payload109).key)
                        mbt_ffi_store32((iter_base) + 8, (payload109).key.length())
                        mbt_ffi_store32((iter_base) + 4, ptr110)
                        mbt_ffi_storef32((iter_base) + 12, (payload109).value)
                        cleanup_list.push(ptr110)

                        ()
                  }
                  Move(payload111) => {
                        mbt_ffi_store8((iter_base) + 0, (8))

                        let ptr112 = mbt_ffi_str2ptr((payload111).key)
                        mbt_ffi_store32((iter_base) + 8, (payload111).key.length())
                        mbt_ffi_store32((iter_base) + 4, ptr112)

                        let ptr113 = mbt_ffi_str2ptr((payload111).parent)
                        mbt_ffi_store32((iter_base) + 16, (payload111).parent.length())
                        mbt_ffi_store32((iter_base) + 12, ptr113)
                        mbt_ffi_store32((iter_base) + 20, ((payload111).index).reinterpret_as_int())
                        cleanup_list.push(ptr112)
                        cleanup_list.push(ptr113)

                        ()
                  }
                  Remove(payload114) => {
                        mbt_ffi_store8((iter_base) + 0, (9))

                        let ptr115 = mbt_ffi_str2ptr(payload114)
                        mbt_ffi_store32((iter_base) + 8, payload114.length())
                        mbt_ffi_store32((iter_base) + 4, ptr115)
                        cleanup_list.push(ptr115)

                        ()
                  }
            }

      }
      let return_area = mbt_ffi_malloc(20)
      wasmImportApply((entity).reinterpret_as_int(), address116, (mutations).length(), return_area);

      let lifted = match (mbt_ffi_load8_u((return_area) + 0)) {
            0 => {

                  Result::Ok(())
            }
            1 => {

                  let result = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 4), mbt_ffi_load32((return_area) + 8))

                  let result118 = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 12), mbt_ffi_load32((return_area) + 16))

                  Result::Err(HostError::{code : result, message : result118})
            }
            _ => panic()
      }
      let ret = lifted
      mbt_ffi_free(address116)
      mbt_ffi_free(return_area)

      cleanup_list.each(mbt_ffi_free)
      return ret

}

