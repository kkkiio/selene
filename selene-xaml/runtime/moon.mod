name = "KKKIIO/selene_xaml_runtime"

version = "0.1.0"

import {
  "KKKIIO/selene@0.37.3",
}

supported_targets = "native+js"

preferred_target = "native"

readme = "../README.md"

repository = "https://github.com/kkkiio/selene_xaml.git"

license = "Apache-2.0"

keywords = [ "XAML", "UI", "ECS", "Game Engine" ]

description = "Selene host runtime for generated Selene XAML views"

source = "src"
